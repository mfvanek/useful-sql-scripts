# Tuning parameters and adding extensions

## Docker Compose
### Start
```shell
docker-compose --project-name="habr-pg-17" up -d
```

### Stop
```shell
docker-compose --project-name="habr-pg-17" down
```
